fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
jinja2>=3.1.0
requests>=2.28.0
python-dotenv>=1.0.0
weasyprint>=62.0
itsdangerous
cryptography>=3.3
SQLAlchemy>=1.4,<2.1
# PostgreSQL driver (used when DATABASE_URL points at Postgres; not needed for the
# default SQLite backend). Safe to keep installed everywhere.
psycopg2-binary>=2.9

# Test-only (not needed at runtime)
pytest>=7.0
httpx>=0.24
