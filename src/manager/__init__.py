"""Manager domain.

Privacy-first: a manager only ever sees a report's goals/1:1s if that report
explicitly opted in (Settings → "Share my goals & 1:1s with my manager") AND set
this manager's email as their manager. Either side can leave comments on an
objective or a 1:1.
"""
